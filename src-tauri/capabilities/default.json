{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults plus the folder picker used by SDK generation output selection.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
